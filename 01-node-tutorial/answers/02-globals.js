// Defined variable in terminal using `export SUPER_SECRET_GLOBAL="George says hello!"`
// If defined, prints "George says hello!", otherwise, prints "undefined"
console.log(process.env.SUPER_SECRET_GLOBAL);

// Prints the path of the current directory
console.log(__dirname);

// Prints the FormData class
console.log(FormData);
